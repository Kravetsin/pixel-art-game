// Game configuration constants

export const TILE_SIZE = 16;
export const SCALE = 3;
export const SCALED_TILE = TILE_SIZE * SCALE;

export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;

export const CHUNK_SIZE = 64;
export const RENDER_DISTANCE = 2; // chunks around the player to keep loaded

export const WORLD_HEIGHT = 256; // tiles
export const SURFACE_LEVEL = 80; // base surface Y in tiles (from top)

export const PLAYER_SPEED = 200;
export const PLAYER_JUMP_VELOCITY = -400;
export const GRAVITY = 800;
